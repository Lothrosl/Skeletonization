To run, Run Ablation.py in trace/models/thinning after running make_data.py

The Analysis comes from, Analysis.py putting the named saved model in and running making sure to name the target/new directory

Project-1.csv holds records of the models hyperparameters.

Evaluation_results holds all the results of the given model. Model 7 is the latest/best model. 

Data holds the data produced by make_data.py

Results holds a full prediction map of each of the 500 intersections chosen including input, target, and predicted.
