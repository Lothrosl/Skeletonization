To run, Run Ablation.py in trace/models/thinning after running make_data.py

The Analysis comes from, Analysis.py putting the named saved model in and running making sure to name the target/new directory
